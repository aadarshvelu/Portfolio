// Config for the Cloudflare adapter (@opennextjs/cloudflare). Left at its
// defaults — this app has no ISR/ image-optimization needs beyond what the
// adapter already wires up automatically.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
